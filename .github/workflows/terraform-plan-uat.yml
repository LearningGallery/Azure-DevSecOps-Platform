name: 'Terraform Plan InfraCode UAT'

on:
  push:
    branches: [ "main" ]
    paths:
      - 'environment/LearningGallery/Infra-IaC-Code/uat/**'
      - '.github/workflows/terraform-plan-uat.yml'
      
  # Manual trigger no longer needs inputs since this file is exclusively UAT
  workflow_dispatch: 

permissions:
  id-token: write
  contents: read

jobs:
  plan-uat:
    name: 'Plan [UAT]'
    runs-on: windows-latest
    environment: uat 
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false
          
      - name: Terraform Format Check
        run: terraform fmt -check -recursive
        working-directory: .\environment\LearningGallery\Infra-IaC-Code\uat
      
      - name: Terraform Init
        run: terraform init
        working-directory: .\environment\LearningGallery\Infra-IaC-Code\uat 

      - name: Run Plan Script
        shell: pwsh
        run: .\environment\LearningGallery\Infra-IaC-Code\uat\Terraform-Scripts\Review-TerraformPlan.ps1 -Action "plan" -Environment "uat" -Path ".\environment\LearningGallery\Infra-IaC-Code\uat"
        env:
          TF_VAR_azure_tenant_id: ${{ secrets.AZURE_TENANT_ID }}
          TF_VAR_windows_admin_password: ${{ secrets.WINDOWS_ADMIN_PASSWORD }}