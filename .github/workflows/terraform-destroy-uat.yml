name: 'Terraform Destroy InfraCode UAT'

# 1. NO automated triggers. This can only be run manually.
on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  destroy-uat:
    name: 'Destroy [UAT]'
    runs-on: windows-latest
    
    # 2. Triggers the UAT Email Approval Gate
    environment: uat 
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false # Keeps PowerShell output clean
          
      - name: Terraform Init
        run: terraform init
        working-directory: .\environment\LearningGallery\Infra-IaC-Code\uat 

      - name: Run Destroy Script
        shell: pwsh
        run: .\environment\LearningGallery\Infra-IaC-Code\uat\Terraform-Scripts\Destroy-Terraform.ps1 -Action "destroy" -Environment "uat" -Path ".\environment\LearningGallery\Infra-IaC-Code\uat"
        env:
          TF_VAR_azure_tenant_id: ${{ secrets.AZURE_TENANT_ID }}
          TF_VAR_windows_admin_password: ${{ secrets.WINDOWS_ADMIN_PASSWORD }}